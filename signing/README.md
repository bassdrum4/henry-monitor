# Android signing key

`henry-monitor.keystore` is the private update identity for this personal Android application. Keep the source archive private and backed up. Android will only install future updates over the existing app when they use this same key.

The password is stored in `password.txt` so this one-off gift remains reproducibly buildable. It is not a Samsung, Google, Windows, or personal-account credential.
